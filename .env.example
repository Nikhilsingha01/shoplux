# Database configuration
DATABASE_URL=postgresql://user:password@host:port/dbname

# Clerk Authentication (If you don't have these, leave them blank and local dev will use dummy keys)
CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Server configuration
PORT=5000
